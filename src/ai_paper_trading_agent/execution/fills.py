from __future__ import annotations


def depth_walk_vwap_fill_placeholder(*, side: str, quantity: float) -> float:
    """Depth-walk VWAP stub.

    M0 intentionally provides a placeholder and explicitly forbids mid-price assumptions.
    """
    raise NotImplementedError(
        "M0 placeholder: implement depth-walk VWAP from L2 book depth; mid-price fills are forbidden."
    )
